<UserControl x:Class="WpfApp.Views.PessoasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Status -->
            <RowDefinition Height="Auto"/>  <!-- Filtro -->
            <RowDefinition Height="*"/>     <!-- Grid -->
            <RowDefinition Height="Auto"/>  <!-- Editor -->
        </Grid.RowDefinitions>

        <!-- Status -->
        <TextBlock Grid.Row="0"
                   Text="{Binding DebugStatus}"
                   Margin="0,0,0,8"/>

        <!-- Filtro simples -->
        <GroupBox Grid.Row="1" Header="Buscar (Nome ou CPF)" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Margin="0,4,10,0"
                         Text="{Binding Filtro, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Grid.Column="1"
                        Content="Buscar"
                        Width="90"
                        Margin="0,4,10,0"
                        Command="{Binding BuscarCommand}"/>

                <Button Grid.Column="2"
                        Content="Limpar"
                        Width="90"
                        Margin="0,4,0,0"
                        Command="{Binding LimparCommand}"/>
            </Grid>
        </GroupBox>

        <!-- Grid -->
        <DataGrid Grid.Row="2"
                  Margin="0,12,0,12"
                  ItemsSource="{Binding Pessoas}"
                  SelectedItem="{Binding Selecionado}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="CPF" Binding="{Binding Cpf}" Width="160"/>
                <DataGridTextColumn Header="Endereço" Binding="{Binding Endereco}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Editor -->
        <GroupBox Grid.Row="3" Header="Cadastro / Edição" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Id:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1"
                         IsReadOnly="True"
                         Margin="0,4,10,0"
                         Text="{Binding Id}"/>

                <TextBlock Grid.Row="0" Grid.Column="2" Text="CPF:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="3"
                         Margin="0,4,10,0"
                         Text="{Binding Cpf, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="1" Grid.Column="0"
                           Text="Nome:"
                           VerticalAlignment="Center"
                           Margin="0,10,0,0"/>
                <TextBox Grid.Row="1" Grid.Column="1"
                         Margin="0,14,10,0"
                         Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="1" Grid.Column="2"
                           Text="End.:"
                           VerticalAlignment="Center"
                           Margin="0,10,0,0"/>
                <TextBox Grid.Row="1" Grid.Column="3"
                         Margin="0,14,10,0"
                         Text="{Binding Endereco, UpdateSourceTrigger=PropertyChanged}"/>

                <StackPanel Grid.Row="0" Grid.Column="4" Grid.RowSpan="2" Orientation="Vertical">
                    <Button Content="Novo"
                            Width="110"
                            Margin="0,4,0,8"
                            Command="{Binding NovoCommand}"/>

                    <Button Content="Salvar"
                            Width="110"
                            Margin="0,0,0,8"
                            Command="{Binding SalvarCommand}"/>

                    <Button Content="Excluir"
                            Width="110"
                            Command="{Binding ExcluirCommand}"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>